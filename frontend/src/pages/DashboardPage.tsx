export default function DashboardPage() {
  // TODO(Phase 1): today's returns, overdue items, quick stats — via
  // apiFetch("/api/bookings/overdue") and apiFetch("/api/bookings/upcoming-returns")
  return <h1>Dashboard</h1>;
}
