# Our own backend API (Railway/Render URL in production)
VITE_API_URL=http://localhost:4000

# Supabase project (safe for the frontend — anon key only, used for Auth)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
